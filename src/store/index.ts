import { createStore } from 'vuex'
import web3Store from "@/modules/web3Store";

export default createStore({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    web3: web3Store,
  }
})
